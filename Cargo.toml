[package]
name = "pi_wifi_connector"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
stdext = "0.3"  # Needed for process execution
